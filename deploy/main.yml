---
- name: Deploy pilab
  hosts: all
  tasks:
    - ansible.builtin.include_role:
        name: "{{ role }}"
      loop:
        - backup
        - common
        - configarr
        - log2ram
        - logrotate
        - needrestart
        - syncthing
        - zerotier
      loop_control:
        loop_var: role
